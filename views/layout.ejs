<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | Codeial</title>

    <!-- Global stylesheet -->
    <link rel="stylesheet" href="/css/layout.css" />
    <!-- Noty CSS -->
    <script src="https://cdn.jsdelivr.net/npm/noty@3.2.0-beta-deprecated/lib/noty.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/noty@3.2.0-beta-deprecated/lib/noty.min.css" rel="stylesheet">
    
    <!-- Page-specific styles (injected dynamically) -->
    <%- style %>
  </head>
  <body>
    
    <div class="app-wrapper">
      <!-- Header -->
      <%- include("_header") %>
      
      <!-- Main content -->
      <main class="app-main">
        <%- body %>
      </main>

      <!-- Footer -->
      <%- include("_footer") %>
    </div>

    <!-- Page-specific scripts (injected dynamically) -->
    <%- script %>
    <script>
      <% if(flash.success && flash.success.length>0){ %>
        new Noty({
          type: "success",
          layout: "topRight",
          text: "<%= flash.success %>",
          timeout: 3000
        }).show();
      <% } %>
    </script>
  </body>
</html>
